---
- hosts: localhost
  gather_facts: false

  tasks:

    - name: test
      debug: 
        msg: 
          - "src={{if vlan_src is defined}}"
          - "vlan_num=={{if vlan_num is defined}}"
          - "vlan_name={{if vlan_name is defined}}"
          - "vlan_ipv4_sub={{vlan_ipv4_sub}}"
          - "vlan_ipv4_gw={{vlan_ipv4_gw}}"
